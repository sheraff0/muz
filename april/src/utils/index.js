import { toPascalName, toCamelCase, 
  removeDuplication, filterOption } from './stringMethods'
import { httpRequest2API } from './httpRequestMethods'
import { dataItem, data2Submit } from './dataMethods'

export {
  toPascalName, toCamelCase, removeDuplication, filterOption,
  httpRequest2API,
  dataItem, data2Submit,
}
